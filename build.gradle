/*
 * This build file was generated by the Gradle 'init' task.
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.4.1/userguide/tutorial_java_projects.html
 */

/*
// Apply the java plugin to add support for Java
apply plugin: 'java'

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'jcenter' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

// In this section you declare the dependencies for your production and test code
dependencies {
    // The production code uses the SLF4J logging API at compile time
    compile 'org.slf4j:slf4j-api:1.7.25'

    // Declare the dependency for your favourite test framework you want to use in your tests.
    // TestNG is also supported by the Gradle Test task. Just change the
    // testCompile dependency to testCompile 'org.testng:testng:6.8.1' and add
    // 'test.useTestNG()' to your build script.
    testCompile 'junit:junit:4.12'
}
*/

plugins {
    id 'de.undercouch.download' version '4.0.0'
    id 'java'
}

version = '0.0.1'

sourceSets {
    main {
         java {
            srcDirs = ['src']
         }
    }

    test {
        java {
            srcDirs = ['test']
        }
        resources {
            srcDirs = ['test']
        }
    }
}

// @seeAlso https://github.com/michel-kraemer/gradle-download-task
task downloadTWSAPIZipFile(type: Download) {
    src 'http://interactivebrokers.github.io/downloads/twsapi_macunix.976.01.zip'
    dest file("$buildDir/dependencies/twsapi.zip")
}

task downloadAndUnzipTWSAPI(dependsOn: downloadTWSAPIZipFile, type: Copy) {
    from zipTree(downloadTWSAPIZipFile.dest)
    into file("$buildDir/dependencies/twsapi/")
}

assemble.dependsOn downloadAndUnzipTWSAPI

task downloadIBCZipFile(type: Download) {
    src 'https://github.com/IbcAlpha/IBC/releases/download/3.8.4-beta.2/IBCLinux-3.8.4-beta.2.zip'
    dest file("$buildDir/dependencies/IBCLinux.zip")
}

task downloadAndUnzipIBC(dependsOn: downloadIBCZipFile, type: Copy) {
    from zipTree(downloadIBCZipFile.dest)
    into file("$buildDir/dependencies/ibclinux/")
}

assemble.dependsOn downloadAndUnzipIBC

repositories {
    mavenCentral()
}

dependencies {
    compile files("$buildDir/dependencies/twsapi/IBJts/source/JavaClient/TwsApi.jar")
    compile files("$buildDir/dependencies/ibclinux/IBC.jar")
    compile 'javax.json:javax.json-api:1.1'
    compile 'org.glassfish:javax.json:1.1'
    compile 'commons-cli:commons-cli:1.4'
    testImplementation 'junit:junit:4.13'
}

compileJava {
    // options.release = 8
    sourceCompatibility = 8
    targetCompatibility = 8
}

jar {
    manifest {
        attributes(
            "Implementation-Title": "IB TWS Shell",
            "Implementation-Version": version,
            "Main-Class": "com.meerkattrading.tws.Bootstrap"
        )
    }
    exclude('com/ib/**')
    from {
		configurations.compile.collect {it.isDirectory() ? it: zipTree(it)}
	}
}

